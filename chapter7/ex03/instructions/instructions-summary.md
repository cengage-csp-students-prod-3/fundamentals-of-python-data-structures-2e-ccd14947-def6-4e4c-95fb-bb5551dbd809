Complete the `Scanner` class located in _scanner.py_ needed to run _evaluatorapp.py_.

In the `Scanner` class of the *scanner.py* file, complete the following:
1. Complete the implementation of the following methods:
	- `__init__`
	- `hasNext()`
	- `next()`
	- `getFirstToken()`
	- `getNextToken()`
	- `nextChar()`
	- `skipWhiteSpace()`
	- `getInteger()`
	
To test your program run the `run()` method in the *evaluatorapp.py* file.

Your program's output should look like the following:
```
Enter a postfix expression: 2 2 *
2 2 * 
4
```